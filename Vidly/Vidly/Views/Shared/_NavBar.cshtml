@using Microsoft.AspNet.Identity
@using Vidly.Models

@* There's a problem when collapse and scale , t scales to the left so the text don't appear
    I want it to scale to right
    also navbar on medium hides content ,I want it to collapse on medium
    It's possible using media queries but It's a pain in the neck, it's builtin in bootstrap 4
    enough styling now
*@
<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @* @Html.ActionLink("Vidly!", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" }) *@
            <a href="@Url.Action("Index", "Home")" class="navbar-brand">
                <span class="fa fa-photo-video"></span> Vidly!
            </a>
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li>@Html.ActionLink("Customers", "Index", "Customers")</li>
                <li>@Html.ActionLink("Movies", "Index", "Movies")</li>
                <li>@Html.ActionLink("Rentals", "Index", "Rentals")</li>
                @if (User.IsInRole(RoleName.CanManageUsers))
                {
                    <li>@Html.ActionLink("Users", "Index", "UsersManagement")</li>

                }
            </ul>
            @Html.Partial("_LoginPartial")
        </div>
    </div>
</div>
@Scripts.Render("~/bundles/fontawesome")
@Styles.Render("~/Content/fontawesome")